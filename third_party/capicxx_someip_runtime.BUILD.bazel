load("@rules_foreign_cc//foreign_cc:defs.bzl", "cmake")

filegroup(
    name = "all_srcs",
    srcs = glob(["**"]),
)

cmake(
    name = "capicxx_someip_runtime",
    build_args = ["-j"],
    out_shared_libs = [
        "libCommonAPI-SomeIP.so",
        "libCommonAPI-SomeIP.so.3.2.4",
    ],
    out_include_dir = "include/CommonAPI-3.2",
    lib_source = ":all_srcs",
    visibility = ["//visibility:public"],
    deps = [
        "@capicxx_core_runtime//:capicxx_core_runtime",
        "@vsomeip//:vsomeip",
    ],
)
